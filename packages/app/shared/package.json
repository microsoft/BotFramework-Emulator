{
  "name": "@bfemulator/app-shared",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "built/index.js",
  "types": "built/index.d.ts",
  "scripts": {
    "build": "tslint --project tsconfig.json && npm run clean && tsc --pretty",
    "clean": "rimraf ./built",
    "lint": "tslint --project tsconfig.json",
    "prepare": "npm run clean",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^22.2.3",
    "jest": "^22.4.4",
    "rimraf": "^2.6.2",
    "ts-jest": "^22.4.6",
    "typescript": "2.7.2",
    "tslint": "^5.10.0"
  },
  "dependencies": {
    "@bfemulator/sdk-shared": "^1.0.0",
    "msbot": "^1.0.44",
    "tslib": "^1.9.0"
  }
}
