{
  "name": "botframework-emulator-shared",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && tsc --pretty",
    "clean": "rimraf ./built"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tslib": "^1.9.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.2"
  }
}
